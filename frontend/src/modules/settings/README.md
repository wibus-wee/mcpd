<!-- Once this directory changes, update this README.md -->

# Modules/Settings

全局运行时配置的设置页面。
通过 bindings 读取与更新 runtime 默认值，并触发配置重载。
设置相关页面与逻辑统一放在该模块中。

## Files

- **settings-page.tsx**: 运行时配置设置页面，包含 bootstrap/activation 默认值的编辑与保存
